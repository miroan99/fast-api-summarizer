# =============================
# requirements.txt
# =============================
# fastapi web framework
# uvicorn ASGI server (\"[standard]\" adds useful extras for dev)
# openai SDK v1
# python-dotenv for .env support
# httpx used by openai client
# pydantic comes with FastAPI, but listed for clarity
#
# Install with: pip install -r requirements.txt

fastapi==0.115.0
uvicorn[standard]==0.30.6
openai==1.41.0
python-dotenv==1.0.1
httpx==0.27.2
pydantic==2.9.2
slowapi==0.1.9
limits==3.13.0
pdfplumber==0.11.4
python-multipart==0.0.9

# Optional: for distributed rate limiting storage (instead of in-memory)
# redis==5.0.8# Optional: for distributed rate limiting storage (instead of in-memory)

# Data science stack
numpy
pandas
matplotlib

# Dev tools
black            # code formatter
ruff             # linter / static analysis

